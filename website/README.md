# Website

Open `index.html` directly in a browser to view the benchmark atlas.

If you want a local server instead of opening the file directly, run:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000/website/`.
